#include "fmath.h"

void initialize_perspective() 
{

}

unsigned int get_value_ptr()
{

}


vector add_vector(vector a, vector b) 
{
	return (vector)
	{
		(a.x + b.x),
		(a.y + b.y),
		(a.z + b.z)
	};
}